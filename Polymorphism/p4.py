# Create a function calculate_area(shape) that takes an object of a class derived from Shape (e.g., Rectangle or Circle) and returns the area of the shape.

def calculate_area(shape):
    return shape.area()
